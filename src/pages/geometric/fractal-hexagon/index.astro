---
import WIPPageTemplate from '../../../components/WIPPageTemplate.astro';
import { getProjectColor } from '../../../utils/projectColors';

const colors = getProjectColor('geometric', 'fractal-hexagon');
---

<WIPPageTemplate 
  title="Hexagon Fractal 1"
  description="Rule: Not the same vertex twice."
  metaDescription="Hexagon fractal visualization using chaos game with rule: cannot select the same vertex twice in a row."
  categoryId="geometric"
  categoryName="Geometric"
  colors={colors}
/>