---
import WIPPageTemplate from '../../../components/WIPPageTemplate.astro';
import { getProjectColor } from '../../../utils/projectColors';

const colors = getProjectColor('geometric', 'fractal-pentagon-2');
---

<WIPPageTemplate 
  title="Pentagon Fractal 2"
  description="Rule: If previous two are same, cannot be neighbors."
  metaDescription="Pentagon fractal with complex rule: if previous two vertices are the same, next cannot be neighbors."
  categoryId="geometric"
  categoryName="Geometric"
  colors={colors}
/>