---
import WIPPageTemplate from '../../../components/WIPPageTemplate.astro';
import { getProjectColor } from '../../../utils/projectColors';

const colors = getProjectColor('geometric', 'fractal-triangle');
---

<WIPPageTemplate 
  title="Sierpinski's Triangle"
  description="Standard chaos game rules."
  metaDescription="Interactive visualization of Sierpinski's Triangle using standard chaos game rules with HTML5 Canvas."
  categoryId="geometric"
  categoryName="Geometric"
  colors={colors}
/>