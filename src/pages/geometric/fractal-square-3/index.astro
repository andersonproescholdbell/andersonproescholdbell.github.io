---
import WIPPageTemplate from '../../../components/WIPPageTemplate.astro';
import { getProjectColor } from '../../../config/projectUtils';

const colors = getProjectColor('geometric', 'fractal-square-3');
---

<WIPPageTemplate 
  title="Square Fractal 3"
  description="Rule: Not vertex 2 away from previous."
  metaDescription="Square fractal visualization with rule: cannot select the vertex 2 positions away from the previous one."
  categoryId="geometric"
  categoryName="Geometric"
  colors={colors}
/>