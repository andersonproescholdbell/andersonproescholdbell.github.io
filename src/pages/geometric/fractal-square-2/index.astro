---
import WIPPageTemplate from '../../../components/WIPPageTemplate.astro';
import { getProjectColor } from '../../../utils/projectColors';

const colors = getProjectColor('geometric', 'fractal-square-2');
---

<WIPPageTemplate 
  title="Square Fractal 2"
  description="Rule: Not clockwise/counter-clockwise of previous."
  metaDescription="Square fractal visualization with rule: cannot select vertex clockwise or counter-clockwise to the previous one."
  categoryId="geometric"
  categoryName="Geometric"
  colors={colors}
/>