---
// Sierpinski's Triangle - Standard Chaos Game Rules
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Sierpinski's Triangle - Standard Rules</title>
    <meta name="description" content="Sierpinski's Triangle generated using standard chaos game rules">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #ec4899 0%, #be185d 100%);
            color: white;
            min-height: 100vh;
            margin: 0;
            padding: 40px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        .container {
            text-align: center;
            max-width: 600px;
        }
        h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            background: linear-gradient(45deg, #f9a8d4, #ffffff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        .status-badge {
            background: rgba(255, 193, 7, 0.2);
            border: 2px solid #ffc107;
            color: #ffc107;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: bold;
            margin: 20px 0;
            display: inline-block;
        }
        .description {
            font-size: 1.2rem;
            line-height: 1.6;
            margin-bottom: 30px;
            opacity: 0.9;
        }
        .back-link {
            color: #f9a8d4;
            text-decoration: none;
            font-weight: bold;
            padding: 12px 24px;
            border: 2px solid #f9a8d4;
            border-radius: 25px;
            transition: all 0.3s ease;
        }
        .back-link:hover {
            background: #f9a8d4;
            color: #be185d;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Sierpinski's Triangle</h1>
        <div class="status-badge">Work in Progress</div>
        <p class="description">
            Interactive visualization of Sierpinski's Triangle using the standard chaos game rules. 
            Watch as the fractal pattern emerges through iterative point plotting on HTML5 Canvas.
        </p>
        <a href="/geometric/" class="back-link">← Back to Geometric Fractals</a>
    </div>
</body>
</html>