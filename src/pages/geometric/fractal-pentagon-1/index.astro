---
import WIPPageTemplate from '../../../components/WIPPageTemplate.astro';
import { getProjectColor } from '../../../config/projectUtils';

const colors = getProjectColor('geometric', 'fractal-pentagon-1');
---

<WIPPageTemplate 
  title="Pentagon Fractal 1"
  description="Rule: Not the same vertex twice."
  metaDescription="Pentagon fractal visualization using chaos game with rule: cannot select the same vertex twice in a row."
  categoryId="geometric"
  categoryName="Geometric"
  colors={colors}
/>