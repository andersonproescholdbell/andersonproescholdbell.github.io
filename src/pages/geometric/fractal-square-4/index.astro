---
import WIPPageTemplate from '../../../components/WIPPageTemplate.astro';
import { getProjectColor } from '../../../utils/projectColors';

const colors = getProjectColor('geometric', 'fractal-square-4');
---

<WIPPageTemplate 
  title="Square Fractal 4"
  description="Rule: If previous two are same, cannot be neighbors."
  metaDescription="Square fractal with complex rule: if previous two vertices are the same, next cannot be neighbors."
  categoryId="geometric"
  categoryName="Geometric"
  colors={colors}
/>