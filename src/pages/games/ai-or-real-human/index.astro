---
// AI or Real Human - Guess if the image is AI-generated or real
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>AI or Real Human - Image Classification Game</title>
    <meta name="description" content="Guess if the image is AI-generated or real human photo">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #0d9488 0%, #0f766e 100%);
            color: white;
            min-height: 100vh;
            margin: 0;
            padding: 40px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        .container {
            text-align: center;
            max-width: 600px;
        }
        h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            background: linear-gradient(45deg, #5eead4, #ffffff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        .status-badge {
            background: rgba(255, 193, 7, 0.2);
            border: 2px solid #ffc107;
            color: #ffc107;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: bold;
            margin: 20px 0;
            display: inline-block;
        }
        .description {
            font-size: 1.2rem;
            line-height: 1.6;
            margin-bottom: 30px;
            opacity: 0.9;
        }
        .back-link {
            color: #5eead4;
            text-decoration: none;
            font-weight: bold;
            padding: 12px 24px;
            border: 2px solid #5eead4;
            border-radius: 25px;
            transition: all 0.3s ease;
        }
        .back-link:hover {
            background: #5eead4;
            color: #0f766e;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>AI or Real Human</h1>
        <div class="status-badge">Work in Progress</div>
        <p class="description">
            Test your ability to distinguish AI-generated images from real human photos. 
            Challenge yourself with this game that explores the current state of AI image generation technology.
        </p>
        <a href="/games/" class="back-link">← Back to Games</a>
    </div>
</body>
</html>